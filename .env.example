# Copy to .env and fill in real values (never commit real secrets)
FLASK_ENV=production
PORT=8000
FLASK_SECRET_KEY=replace_me

# If you use Gemini or OpenAI, set one of the following:
GEMINI_API_KEY=replace_me    # required for AI
OPENAI_API_KEY=replace_me

# Optional Gemini knobs (safe defaults if omitted):
# GEMINI_MODEL=               # leave blank to auto-use gemini-2.5-flash; options: flash | pro 
# GEMINI_API_VERSION=v1       # recommended for generateContent, v1 (stable) / v1beta; defaults to v1 if omitted

# Do NOT set GEMINI_URL (the app derives it), unless you know you need a custom endpoint.

# Rate limit storage (dev default)
RATELIMIT_STORAGE_URI=memory://

# For production, uncomment and point to your Redis instance:
# RATELIMIT_STORAGE_URI=redis://localhost:6379/0

DATA_PROVIDER=alphavantage
ALPHAVANTAGE_API_KEY=YOUR_KEY_HERE
